[project]
name = "jwt-sample"
version = "1.0.0"
description = "Demonstração prática de JWT em Python"
requires-python = ">=3.8"
dependencies = [
    "cryptography>=41.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"